import { createMuiTheme } from '@material-ui/core/styles';


const Theme = createMuiTheme({
    palette: {
        primary: {
          main: '#6865FF',
        }
    },
    typography: {
      title: {
        fontFamily: 'Pattaya'
      }
    }
})

export default Theme

